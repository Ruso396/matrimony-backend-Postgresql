const path = require('path');

module.exports = {
  'config': path.resolve('src/config', 'config.js'),         // DB config file
  'models-path': path.resolve('src', 'models'),              // Sequelize models
  'seeders-path': path.resolve('src', 'seeders'),            // Seed files
  'migrations-path': path.resolve('src', 'migrations')       // Migration files
};
